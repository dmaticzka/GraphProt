#!/usr/bin/env Rscript

library(ROCR)

f <- file("stdin")
d <- read.table(f, col.names=c("labels", "scores"))
auc <- performance(prediction(d$scores, d$labels), "auc")@y.values[[1]]
cat(paste("ROC",auc,sep="\t"), "\n")
